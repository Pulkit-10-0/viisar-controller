<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.VIISAR" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.VIISAR.Splash" parent="Theme.SplashScreen">

        <!-- Background color -->
        <item name="windowSplashScreenBackground">@color/white</item>

        <!-- Logo -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>

        <!-- After splash -->
        <item name="postSplashScreenTheme">@style/Theme.VIISAR</item>

    </style>
</resources>
